#! clean build -dtSa "-O2 -fPIC" -p test -l -pthread

name: mkn.ram

parent: lib

profile:
- name: base
  inc: inc
  src: src/any
  if_src:
    bsd: src/nixish
    nix: src/nixish
    win: src/win

- name: lib
  parent: base
  dep: mkn.kul
  if_arg:
    win_shared: -DMKN_KUL_SHARED -DMKN_KUL_EXPORT

- name: https
  parent: lib
  arg: -DMKN_RAM_INCLUDE_HTTPS
  if_mod:
    win:
    - name: conan.install
      init:
        install: -s compiler="msvc"
  if_lib:
    nix: ssl crypto
    bsd: ssl crypto
    win: User32 Crypt32 libssl libcrypto zlib
  if_link:
    win_cl: -nodefaultlib:libucrt.lib ucrt.lib

- name: fcgi
  parent: lib
  arg: -DKUL_INCLUDE_FCGI

- name: http2
  parent: https
  arg: -D_MKN_RAM_INCLUDE_HTTP2_

- name: test.http2
  parent: http2
  main: tst/http2.cpp

- name: usage
  parent: https
  main: tst/usage.cpp
  # arg: -DMKN_RAM_INCLUDE_HTTPS
  # if_link:
  #   win_cl: -nodefaultlib:libucrt.lib ucrt.lib

- name: test_fcgi
  main: tst/fcgi.cpp
  self: fcgi

- name: test
  parent: https
  main: tst/test.cpp

- name: test.client
  parent: lib
  main: tst/client.cpp

- name: test.server
  parent: lib
  main: tst/server.cpp

- name: format
  mod:
  - name: clang.format
    init:
      style: file
      types: cpp cxx cc h hpp
      paths: .
      args:  -DMKN_RAM_INCLUDE_HTTPS

#! clean build -dtSp test

name: mkn.ram

parent: lib

profile:
  - name: base
    inc: ./inc
    if_inc:
        bsd: ./os/nixish/inc
        nix: ./os/nixish/inc
        win: ./os/win/inc

  - name: lib
    parent: base
    src: ./src
    if_src:
        bsd: ./os/nixish/src
        nix: ./os/nixish/src
        win: ./os/win/src
    dep:
      - name: mkn.kul
        version: master

  - name: c++0x
    parent: base
    dep:
      - name: mkn.kul
        version: master
        profile: c++0x

  - name: test
    mode: none
    main: test.cpp
    self: lib

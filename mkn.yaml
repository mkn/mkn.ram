#! clean build -dtSp test

name: mkn.ram

parent: build

profile:
  - name: build
    inc: ./inc
    dep:
      - name: mkn.kul
        version: 21062015
    if_inc:
        bsd: ./os/nixish/inc
        nix: ./os/nixish/inc
        win: ./os/win/inc
    if_src:
        win: ./os/win/src
    if_lib:
        nix: pthread

  - name: test
    parent: build
    mode: none
    main: test.cpp
